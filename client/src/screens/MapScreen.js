import React from 'react'
import { View, StyleSheet, Text } from 'react-native'

const MapScreen = () => {
    return <Text style={{ fontSize: 48 }}>Map Screen</Text> 
}

const styles = StyleSheet.create({})

export default MapScreen